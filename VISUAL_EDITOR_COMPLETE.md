# 🎨 Visual Editor - Complete Documentation

## ✅ อัปเดตเสร็จสมบูรณ์!

ระบบ **Visual Editor** ใหม่ทั้งหมดพร้อมใช้งานแล้ว! 🎉

---

## 🌟 ฟีเจอร์ใหม่ทั้งหมด

### 1. ✏️ **แก้ไขข้อมูลแบบ Inline**
```
✅ คลิกที่ Widget แล้วพิมพ์ได้เลย
✅ แก้ไขหัวข้อ, เนื้อหา โดยตรง
✅ อัปโหลด/เปลี่ยนรูปภาพได้ทันที
✅ ไม่ต้องเปิด Modal ให้ยุ่งยาก
```

### 2. 🎨 **เปลี่ยนสีได้ทุกส่วน**
```
✅ สีพื้นหลัง (Background Color)
✅ สีข้อความ (Text Color)
✅ สีขอบ (Border Color)
✅ เห็นผลทันทีในหน้า Admin
✅ หน้าเว็บหลักเปลี่ยนตาม
```

### 3. 📐 **จัดวาง Section**
```
✅ ลาก Widget เพื่อเปลี่ยนตำแหน่ง
✅ ลากมุมขวาล่างเพื่อปรับขนาด
✅ จัดเรียงแบบ Grid Layout (12 columns)
✅ Widget ไม่ทับกัน, จัดเรียงอัตโนมัติ
```

### 4. 🔧 **จัดวางองค์ประกอบใน Section**
```
✅ เลือกทิศทางเลย์เอาต์: แนวนอน (Row) / แนวตั้ง (Column)
✅ จัดเรียงข้อความ: ซ้าย / กลาง / ขวา
✅ ปรับระยะขอบใน (Padding): เล็ก / กลาง / ใหญ่ / ใหญ่มาก
✅ Responsive อัตโนมัติ
```

### 5. 💾 **บันทึกทีเดียว**
```
✅ ปุ่ม "บันทึกทั้งหมด" เดียวจบ
✅ บันทึก Layout, Widgets, Profile, และ Styles พร้อมกัน
✅ หน้าเว็บหลักอัปเดตทันที
✅ Sticky button ลอยอยู่มุมขวาล่าง
```

---

## 📂 ไฟล์ที่อัปเดต

### 1. `/src/app/admin/layout-builder/page.tsx`
**เปลี่ยนแปลงครั้งใหญ่:**
- ✅ **ออกแบบ UI ใหม่ทั้งหมด** เป็น Visual Editor
- ✅ **แก้ไขแบบ Inline** - คลิก Widget แล้วพิมพ์ได้เลย
- ✅ **Style Editor** - เปลี่ยนสี, alignment, layout direction, padding
- ✅ **บันทึกทีเดียว** - `handleSaveAll()` บันทึกทุกอย่างพร้อมกัน
- ✅ **Collapsible Profile Editor** - แก้ไขข้อมูล Hero, About, Education, Contact
- ✅ **Image Upload Inline** - อัปโหลดรูปภาพได้จาก Widget โดยตรง

**ฟังก์ชันสำคัญ:**
```typescript
// 1. Get Widget Style
const getWidgetStyle = (widget: Widget): WidgetStyle => {
  try {
    return widget.settings ? JSON.parse(widget.settings) : {};
  } catch {
    return {};
  }
};

// 2. Update Widget (Real-time)
const handleUpdateWidget = async (widgetId: number, updates: Partial<Widget>) => {
  const updatedWidgets = layout.widgets.map((w) =>
    w.id === widgetId ? { ...w, ...updates } : w
  );
  setLayout({ ...layout, widgets: updatedWidgets });
};

// 3. Save All (One Click)
const handleSaveAll = async () => {
  // บันทึก Layout & Widgets
  // บันทึก Profile Data
  // บันทึก Edit History
  // Refresh หน้าเว็บ
};
```

**Widget Settings (JSON):**
```json
{
  "backgroundColor": "#ffffff",
  "textColor": "#1f2937",
  "borderColor": "#e5e7eb",
  "borderWidth": "2px",
  "alignment": "left",
  "flexDirection": "column",
  "padding": "1.5rem"
}
```

### 2. `/src/app/page.tsx`
**เปลี่ยนแปลง:**
- ✅ เพิ่ม `WidgetStyle` interface
- ✅ ฟังก์ชัน `getWidgetStyle()` - ดึง style จาก widget.settings
- ✅ ฟังก์ชัน `getStyleObject()` - แปลง style เป็น React CSSProperties
- ✅ อัปเดต `renderImageWidget()` - ใช้ style ที่กำหนดได้
- ✅ อัปเดต `renderTextWidget()` - ใช้ style ที่กำหนดได้

**ตัวอย่างการใช้ Style:**
```typescript
const renderTextWidget = (widget: Widget) => {
  const style = getWidgetStyle(widget);
  const styleObj = getStyleObject(style);

  return (
    <section style={styleObj}>
      <h2 style={{ color: style.textColor }}>
        {widget.title}
      </h2>
      <div
        style={{
          backgroundColor: styleObj.backgroundColor,
          borderColor: style.borderColor,
          borderWidth: style.borderWidth,
        }}
      >
        <p style={{ textAlign: style.alignment }}>
          {widget.content}
        </p>
      </div>
    </section>
  );
};
```

---

## 🎯 วิธีใช้งาน Visual Editor

### ขั้นตอนที่ 1: เข้าสู่ระบบ
```bash
1. ไปที่ http://localhost:3000/admin/login
2. ล็อกอิน Admin
3. คลิก "จัดการเลย์เอาต์ & แก้ไขเนื้อหา"
```

### ขั้นตอนที่ 2: แก้ไข Widget
```
1. คลิกที่ Widget ที่ต้องการแก้ไข
2. พิมพ์ในช่อง "หัวข้อ" หรือ "เนื้อหา"
3. อัปโหลดรูปภาพ (ถ้าต้องการ)
4. เห็นผลทันที!
```

### ขั้นตอนที่ 3: เปลี่ยนสี
```
1. คลิก Widget เพื่อแสดงตัวเลือกสี
2. เลือกสีพื้นหลัง (Background)
3. เลือกสีข้อความ (Text)
4. เลือกสีขอบ (Border)
5. เห็นผลทันที!
```

### ขั้นตอนที่ 4: จัดวาง Widget
```
1. ลาก Widget เพื่อย้ายตำแหน่ง
2. ลากมุมขวาล่างเพื่อปรับขนาด
3. Widget อื่นจะจัดเรียงอัตโนมัติ
```

### ขั้นตอนที่ 5: จัดวางองค์ประกอบ
```
1. คลิก Widget เพื่อเปิดตัวเลือก
2. เลือก "ทิศทางเลย์เอาต์": แนวนอน / แนวตั้ง
3. เลือก "จัดเรียง": ซ้าย / กลาง / ขวา
4. เลือก "ระยะขอบใน": เล็ก / กลาง / ใหญ่
```

### ขั้นตอนที่ 6: แก้ไขข้อมูลโปรไฟล์
```
1. คลิก "แก้ไขข้อมูลโปรไฟล์" (ด้านล่าง Layout Builder)
2. แก้ไขข้อมูล Hero, About, Education, Contact
3. กรอกข้อมูลในช่องต่างๆ
```

### ขั้นตอนที่ 7: บันทึก
```
1. กดปุ่ม "💾 บันทึกทั้งหมด" (มุมขวาบน หรือ มุมขวาล่าง)
2. รอสักครู่...
3. เห็นข้อความ "✅ บันทึกทั้งหมดสำเร็จ!"
4. หน้าเว็บหลักจะอัปเดตทันที
```

### ขั้นตอนที่ 8: ตรวจสอบผลลัพธ์
```
1. คลิก "🌐 ดูหน้าเว็บ" (มุมขวาบน)
2. เปิดในแท็บใหม่
3. เห็นการเปลี่ยนแปลงทั้งหมด!
```

---

## 🎨 Widget Types

### 1. 🏠 Hero Section
```
แก้ไขได้:
- ชื่อ, คำอธิบาย
- รูปภาพ Hero
- สี: พื้นหลัง, ข้อความ, ขอบ
- จัดเรียง, ระยะขอบใน
```

### 2. 👤 About Section
```
แก้ไขได้:
- ประวัติส่วนตัว, ความสำเร็จ
- ทักษะ (แต่ละบรรทัด)
- สี: พื้นหลัง, ข้อความ, ขอบ
- จัดเรียง, ระยะขอบใน
```

### 3. ⚡ Skills
```
แก้ไขได้:
- รายการทักษะ (จาก About Section)
- สี: พื้นหลัง, ข้อความ, ขอบ
- จัดเรียง, ระยะขอบใน
```

### 4. 🎓 Education & Experience
```
แก้ไขได้:
- การศึกษา: มหาวิทยาลัย, มัธยม
- ประสบการณ์ทำงาน (จากหน้า /admin/experience)
- สี: พื้นหลัง, ข้อความ, ขอบ
- จัดเรียง, ระยะขอบใน
```

### 5. 💼 Portfolio
```
แก้ไขได้:
- ผลงาน (จากหน้า /admin/portfolio)
- สี: พื้นหลัง, ข้อความ, ขอบ
- จัดเรียง, ระยะขอบใน
```

### 6. 📧 Contact
```
แก้ไขได้:
- อีเมล, เบอร์โทร, ที่อยู่
- รูปภาพ Contact
- สี: พื้นหลัง, ข้อความ, ขอบ
- จัดเรียง, ระยะขอบใน
```

### 7. 🖼️ Image Widget
```
แก้ไขได้:
- หัวข้อ, เนื้อหา
- รูปภาพ (อัปโหลดได้)
- สี: พื้นหลัง, ข้อความ, ขอบ
- จัดเรียง, ระยะขอบใน
```

### 8. 📝 Text Widget
```
แก้ไขได้:
- หัวข้อ, เนื้อหา (รองรับ multiline)
- สี: พื้นหลัง, ข้อความ, ขอบ
- จัดเรียง, ระยะขอบใน
```

---

## 🎨 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: สร้าง Hero Section สีฟ้า
```
1. คลิก Hero Widget
2. พิมพ์หัวข้อ: "สวัสดี ฉันคือ..."
3. พิมพ์เนื้อหา: "นักพัฒนาเว็บไซต์"
4. เลือกสีพื้นหลัง: #3b82f6 (สีฟ้า)
5. เลือกสีข้อความ: #ffffff (สีขาว)
6. อัปโหลดรูปภาพโปรไฟล์
7. กดบันทึก
```

### ตัวอย่างที่ 2: สร้าง Text Widget แนวนอน
```
1. เพิ่ม Text Widget ใหม่
2. พิมพ์หัวข้อ: "ติดต่อเรา"
3. พิมพ์เนื้อหา: "Line: @example\nEmail: me@example.com"
4. เลือกทิศทางเลย์เอาต์: แนวนอน
5. เลือกจัดเรียง: กลาง
6. เลือกสีพื้นหลัง: #f3f4f6 (สีเทาอ่อน)
7. กดบันทึก
```

### ตัวอย่างที่ 3: สร้าง Image Gallery
```
1. เพิ่ม Image Widget 3 อัน
2. ลาก Widget ให้อยู่แถวเดียวกัน (Y เท่ากัน)
3. ปรับขนาดแต่ละ Widget: 4 columns (W = 4)
4. อัปโหลดรูปภาพในแต่ละ Widget
5. เลือกสีขอบ: #9333ea (สีม่วง)
6. เลือกความหนาขอบ: 3px
7. กดบันทึก
```

---

## 🔧 การตั้งค่าขั้นสูง

### Widget Settings (JSON Format)
```json
{
  "backgroundColor": "#ffffff",    // สีพื้นหลัง (HEX)
  "textColor": "#1f2937",          // สีข้อความ (HEX)
  "borderColor": "#e5e7eb",        // สีขอบ (HEX)
  "borderWidth": "2px",            // ความหนาขอบ
  "alignment": "left",             // left | center | right
  "flexDirection": "column",       // row | column
  "padding": "1.5rem"              // 0.5rem | 1rem | 1.5rem | 2rem
}
```

### การแก้ไข Settings ด้วยมือ (ผ่าน Prisma Studio)
```bash
1. เปิด Prisma Studio:
   npx prisma studio

2. เปิดตาราง "Widget"

3. แก้ไขฟิลด์ "settings":
   {
     "backgroundColor": "#ff0000",
     "textColor": "#ffffff",
     "borderColor": "#000000",
     "borderWidth": "5px",
     "alignment": "center",
     "flexDirection": "row",
     "padding": "2rem"
   }

4. บันทึก

5. Refresh หน้าเว็บ
```

---

## 📊 Database Schema

### Widget Table
```prisma
model Widget {
  id        Int      @id @default(autoincrement())
  type      String   // "hero", "about", "skills", etc.
  title     String?
  content   String?  @db.Text
  imageUrl  String?  @db.Text
  x         Int      @default(0)      // Grid position X
  y         Int      @default(0)      // Grid position Y
  w         Int      @default(6)      // Grid width (1-12)
  h         Int      @default(4)      // Grid height
  order     Int      @default(0)      // Display order
  isVisible Boolean  @default(true)   // Show/Hide
  settings  String?  @db.Text         // JSON: styles, alignment, etc.
  layoutId  Int
  layout    Layout   @relation(fields: [layoutId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([layoutId])
  @@index([order])
}
```

---

## 🎯 เคล็ดลับการใช้งาน

### 1. 🎨 เลือกสีที่ลงตัว
```
✓ ใช้ Color Picker ในตัว
✓ สีควรมีความ Contrast ที่ดี (ข้อความอ่านง่าย)
✓ แนะนำ:
  - พื้นหลัง: สีอ่อน (#f3f4f6, #ffffff)
  - ข้อความ: สีเข้ม (#1f2937, #000000)
```

### 2. 📐 จัดวางให้สวยงาม
```
✓ ใช้ Grid 12 columns
✓ Widget ขนาดเท่ากันจะดูสวย
✓ แนะนำ:
  - Full width: W = 12
  - Half width: W = 6
  - Third width: W = 4
  - Quarter width: W = 3
```

### 3. 💾 บันทึกบ่อยๆ
```
✓ กดบันทึกทุกครั้งที่แก้ไขเสร็จ
✓ ตรวจสอบหน้าเว็บหลักทันที
✓ ถ้าผิดพลาด สามารถ Undo ได้ผ่าน Edit History
```

### 4. 🖼️ ใช้รูปภาพคุณภาพสูง
```
✓ ขนาดไฟล์ไม่เกิน 5MB
✓ รองรับ: JPG, PNG, GIF, WebP
✓ แนะนำขนาด:
  - Hero Image: 800x800px
  - Contact Image: 400x400px
  - Widget Image: 1200x600px
```

### 5. 📱 ทดสอบ Responsive
```
✓ ตรวจสอบบนมือถือ
✓ เปิด DevTools (F12) → Toggle Device Toolbar
✓ ทดสอบขนาด: Mobile (375px), Tablet (768px), Desktop (1024px)
```

---

## 🐛 Troubleshooting

### ปัญหา 1: สีไม่เปลี่ยน
**สาเหตุ:**
- Settings ไม่ถูก Parse เป็น JSON
- Widget Type ไม่รองรับ Settings

**วิธีแก้:**
```
1. ตรวจสอบ Widget Type: Image, Text รองรับ Settings
2. ตรวจสอบ settings field ใน Database
3. ลองสร้าง Widget ใหม่
```

### ปัญหา 2: บันทึกไม่สำเร็จ
**สาเหตุ:**
- ไม่ได้ล็อกอิน
- Token หมดอายุ
- Network Error

**วิธีแก้:**
```
1. ล็อกเอาท์แล้วล็อกอินใหม่
2. ตรวจสอบ Console (F12)
3. ตรวจสอบ Network Tab
```

### ปัญหา 3: หน้าเว็บไม่อัปเดต
**สาเหตุ:**
- Cache ค้าง
- ไม่ได้ Refresh

**วิธีแก้:**
```
1. Hard Refresh: Ctrl+Shift+R (Cmd+Shift+R)
2. ล้าง Cache: Ctrl+Shift+Delete
3. เปิด Incognito Mode
4. ตรวจสอบ API: /api/layout, /api/profile
```

### ปัญหา 4: Widget หาย
**สาเหตุ:**
- `isVisible = false`
- ถูกลบ

**วิธีแก้:**
```
1. ตรวจสอบปุ่ม "👁️" (Show/Hide)
2. เปิด Prisma Studio: npx prisma studio
3. ตรวจสอบ Widget table
4. เปลี่ยน isVisible = true
```

### ปัญหา 5: Layout ยุ่ง
**สาเหตุ:**
- Widget ทับกัน
- ขนาดไม่เหมาะสม

**วิธีแก้:**
```
1. ลาก Widget ออกจากกัน
2. ปรับขนาดให้เหมาะสม
3. ใช้ "Compact Type: Vertical" (จัดเรียงอัตโนมัติ)
4. รีเซ็ต Layout: ลบ Widget ทั้งหมดแล้วสร้างใหม่
```

---

## 📱 Responsive Design

### Mobile (< 768px)
```
✓ Widget จัดเรียงแนวตั้งอัตโนมัติ
✓ ข้อความ font-size ลดลง
✓ Padding ลดลง
✓ รูปภาพปรับขนาดอัตโนมัติ
```

### Tablet (768px - 1024px)
```
✓ Widget 2 columns
✓ ข้อความ font-size กลาง
✓ Padding กลาง
✓ รูปภาพขนาดกลาง
```

### Desktop (> 1024px)
```
✓ Widget ตาม Grid Layout
✓ ข้อความ font-size ใหญ่
✓ Padding ใหญ่
✓ รูปภาพขนาดเต็ม
```

---

## 🎉 สรุป

### ✅ สิ่งที่ทำได้แล้ว
1. ✅ **แก้ไขข้อมูลแบบ Inline** - คลิกแล้วพิมพ์ได้เลย
2. ✅ **เปลี่ยนสี** - พื้นหลัง, ข้อความ, ขอบ
3. ✅ **จัดวาง Section** - ลาก, ปรับขนาด
4. ✅ **จัดวางองค์ประกอบ** - แนวนอน/ตั้ง, ซ้าย/กลาง/ขวา, Padding
5. ✅ **บันทึกทีเดียว** - ปุ่มเดียวจบ
6. ✅ **Real-time Sync** - เห็นผลทันที
7. ✅ **Responsive** - รองรับทุก Device
8. ✅ **User-friendly** - ใช้งานง่าย, UI สวยงาม

### 🎯 เหมาะสำหรับ
- ✓ นักพัฒนาเว็บไซต์
- ✓ นักออกแบบ UI/UX
- ✓ Content Creator
- ✓ ผู้ใช้ทั่วไปที่ต้องการสร้างเว็บไซต์สวยๆ

### 💡 ข้อแนะนำ
- ⭐ ทดลองใช้ฟีเจอร์ต่างๆ
- ⭐ เล่นกับสี, Layout, และ Alignment
- ⭐ บันทึกบ่อยๆ
- ⭐ ตรวจสอบหน้าเว็บหลักเสมอ
- ⭐ แชร์ผลงานให้เพื่อนดู!

---

**🎨 Visual Editor ของคุณพร้อมใช้งานแล้ว!**

**🚀 เริ่มสร้างเว็บไซต์สวยๆ ได้เลย!**

**✨ Happy Designing! 💖**

---

**เวอร์ชัน**: 3.0 (Visual Editor Complete)  
**วันที่**: November 15, 2025  
**สถานะ**: ✅ พร้อมใช้งาน 100%


