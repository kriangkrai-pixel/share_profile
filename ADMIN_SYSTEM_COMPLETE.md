# 🎨 ระบบ Admin ใหม่ - คู่มือฉบับสมบูรณ์

## 📋 ภาพรวมระบบ

ระบบ Admin นี้ออกแบบมาเพื่อให้คุณสามารถจัดการเว็บไซต์ Profile ของคุณได้อย่างง่ายดาย โดยไม่ต้องแก้ไข Code เลย!

### 🎯 จุดประสงค์หลัก
- **แก้ไขเว็บไซต์แบบ Real-time** - เห็นผลทันทีที่แก้ไข
- **จัดการเนื้อหา** - ข้อความ รูปภาพ สี
- **จัดการผลงาน** - เพิ่ม แก้ไข ลบ Portfolio
- **รับข้อความจากลูกค้า** - ดูข้อความที่ส่งมาจากฟอร์มติดต่อ
- **ปรับแต่งสี** - เปลี่ยนสีธีมเว็บไซต์

---

## 🗂️ โครงสร้างระบบ

### 1. **Database Schema** (`prisma/schema.prisma`)

#### 📌 คืออะไร?
Schema เป็นโครงสร้างของฐานข้อมูล กำหนดว่าจะเก็บข้อมูลอะไรบ้าง

#### 📊 ตารางที่เพิ่มมาใหม่:

##### **ContactMessage** - เก็บข้อความติดต่อ
```prisma
model ContactMessage {
  id        Int      @id @default(autoincrement())  // รหัสข้อความ (Auto)
  name      String                                  // ชื่อผู้ส่ง
  email     String                                  // อีเมลผู้ส่ง
  message   String   @db.Text                       // ข้อความ
  isRead    Boolean  @default(false)                // อ่านแล้ว/ยังไม่อ่าน
  createdAt DateTime @default(now())                // วันที่ส่ง
  
  @@index([isRead])      // Index สำหรับค้นหาข้อความที่ยังไม่อ่าน
  @@index([createdAt])   // Index สำหรับเรียงลำดับตามวันที่
}
```

**เอาไว้ทำไร?**
- เก็บข้อความที่ลูกค้าส่งมาจากฟอร์มติดต่อในหน้าเว็บ
- ผู้ดูแลสามารถดูข้อความใน Admin Panel
- ระบุว่าข้อความไหนอ่านแล้ว/ยังไม่อ่าน

**ตัวอย่างข้อมูล:**
```
id: 1
name: "สมชาย ใจดี"
email: "somchai@email.com"
message: "สนใจผลงานของคุณมากครับ ติดต่อกลับด้วยนะครับ"
isRead: false
createdAt: 2025-11-16 14:30:00
```

##### **SiteSettings** - ตั้งค่าสีธีมเว็บไซต์
```prisma
model SiteSettings {
  id              Int      @id @default(autoincrement())
  primaryColor    String   @default("#3b82f6")  // สีหลัก (น้ำเงิน)
  secondaryColor  String   @default("#8b5cf6")  // สีรอง (ม่วง)
  accentColor     String   @default("#10b981")  // สีเน้น (เขียว)
  backgroundColor String   @default("#ffffff")  // สีพื้นหลัง (ขาว)
  textColor       String   @default("#1f2937")  // สีข้อความ (เทาเข้ม)
  headerBgColor   String   @default("#ffffff")  // สีพื้นหลัง Header
  footerBgColor   String   @default("#1f2937")  // สีพื้นหลัง Footer
  updatedAt       DateTime @updatedAt           // วันที่อัปเดตล่าสุด
}
```

**เอาไว้ทำไร?**
- เก็บการตั้งค่าสีของเว็บไซต์
- สามารถเปลี่ยนสีธีมทั้งเว็บได้จากหน้า Admin
- ใช้ได้กับทุกหน้าของเว็บไซต์

**ตัวอย่างการใช้งาน:**
```typescript
// ดึงสีมาใช้ในหน้าเว็บ
const settings = await prisma.siteSettings.findFirst();
// settings.primaryColor = "#3b82f6" (สีน้ำเงิน)
// เอาไปใช้กับ CSS/Tailwind
```

---

### 2. **API Routes** - ตัวกลางระหว่าง Frontend และ Database

#### 📌 `/api/contact/route.ts` - จัดการข้อความติดต่อ

**คืออะไร?**
API Endpoint สำหรับบันทึกและดึงข้อความที่ลูกค้าส่งมา

**ฟังก์ชันที่มี:**

##### **POST** - บันทึกข้อความใหม่
```typescript
// เมื่อลูกค้ากรอกฟอร์มติดต่อในหน้าเว็บ
fetch('/api/contact', {
  method: 'POST',
  body: JSON.stringify({
    name: "สมชาย",
    email: "somchai@email.com",
    message: "สนใจผลงานของคุณ"
  })
});
```

**เอาไว้ทำไร?**
- รับข้อมูลจากฟอร์มติดต่อในหน้าเว็บ
- บันทึกลงฐานข้อมูล
- ส่ง Response กลับว่าบันทึกสำเร็จหรือไม่

##### **GET** - ดึงข้อความทั้งหมด
```typescript
// ดึงข้อความทั้งหมด
fetch('/api/contact');

// ดึงเฉพาะข้อความที่ยังไม่อ่าน
fetch('/api/contact?unreadOnly=true');
```

**เอาไว้ทำไร?**
- แสดงรายการข้อความใน Admin Panel
- กรองข้อความที่ยังไม่อ่าน
- แสดงจำนวนข้อความใหม่

##### **PUT** - อัปเดตสถานะข้อความ
```typescript
// ทำเครื่องหมายว่าอ่านแล้ว
fetch('/api/contact', {
  method: 'PUT',
  body: JSON.stringify({
    id: 1,
    isRead: true
  })
});
```

**เอาไว้ทำไร?**
- เปลี่ยนสถานะข้อความจาก "ยังไม่อ่าน" เป็น "อ่านแล้ว"
- จัดระเบียบข้อความในระบบ

##### **DELETE** - ลบข้อความ
```typescript
// ลบข้อความ
fetch('/api/contact?id=1', {
  method: 'DELETE'
});
```

**เอาไว้ทำไร?**
- ลบข้อความที่ไม่ต้องการออกจากระบบ

---

#### 📌 `/api/settings/route.ts` - จัดการสีธีม

**คืออะไร?**
API Endpoint สำหรับดึงและอัปเดตการตั้งค่าสีของเว็บไซต์

##### **GET** - ดึงการตั้งค่าปัจจุบัน
```typescript
fetch('/api/settings');
// Response: { primaryColor: "#3b82f6", ... }
```

**เอาไว้ทำไร?**
- ดึงสีที่ตั้งค่าไว้มาใช้ในหน้าเว็บ
- แสดงสีปัจจุบันในหน้า Theme Settings
- ถ้ายังไม่มีการตั้งค่า จะสร้างค่าเริ่มต้นให้

##### **PUT** - อัปเดตการตั้งค่า
```typescript
fetch('/api/settings', {
  method: 'PUT',
  body: JSON.stringify({
    primaryColor: "#ff0000",    // เปลี่ยนสีหลักเป็นสีแดง
    secondaryColor: "#00ff00",  // เปลี่ยนสีรองเป็นสีเขียว
  })
});
```

**เอาไว้ทำไร?**
- บันทึกสีที่เลือกใหม่ลงฐานข้อมูล
- เปลี่ยนสีของเว็บไซต์ทั้งหมดพร้อมกัน

---

### 3. **Admin Pages** - หน้าจัดการเว็บไซต์

#### 📌 `/admin/dashboard/page.tsx` - หน้าแรกของ Admin

**คืออะไร?**
หน้า Dashboard หลักที่แสดงภาพรวมและเมนูทั้งหมด

**ประกอบด้วย:**

##### **Stats Cards** - แสดงสถิติ
```typescript
const [stats, setStats] = useState({
  portfolios: 0,       // จำนวนผลงานทั้งหมด
  experiences: 0,      // จำนวนประสบการณ์
  unreadMessages: 0,   // จำนวนข้อความที่ยังไม่อ่าน
});
```

**เอาไว้ทำไร?**
- แสดงจำนวนผลงาน ประสบการณ์ และข้อความใหม่
- ให้ภาพรวมของเว็บไซต์
- มี Badge แจ้งเตือนถ้ามีข้อความใหม่

##### **Menu Grid** - เมนูหลัก 6 รายการ

1. **🎨 แก้ไขหน้าเว็บ** → `/admin/editor`
   - แก้ไขข้อความ รูปภาพ สี แบบ Live Preview
   - เห็นผลทันทีขณะแก้ไข

2. **👤 เกี่ยวกับเรา** → `/admin/about`
   - แก้ไขข้อมูลส่วนตัว
   - ประวัติ ทักษะ ความสำเร็จ

3. **🎓 การศึกษาและประสบการณ์** → `/admin/education-experience`
   - จัดการข้อมูลการศึกษา
   - เพิ่ม แก้ไข ลบ ประสบการณ์การทำงาน

4. **💼 ผลงาน** → `/admin/portfolios`
   - จัดการผลงานทั้งหมด
   - อัปโหลดรูปภาพ เพิ่มลิงก์

5. **📧 ข้อความติดต่อ** → `/admin/messages`
   - ดูข้อความจากลูกค้า
   - ทำเครื่องหมายอ่านแล้ว/ลบ

6. **🎨 ตั้งค่าสีธีม** → `/admin/theme`
   - เปลี่ยนสีของเว็บไซต์
   - เห็นผลแบบ Real-time

**ฟังก์ชันสำคัญ:**

```typescript
// โหลดสถิติ
const loadStats = async () => {
  // ดึงจำนวน Portfolio
  const portfolioRes = await fetch("/api/profile/portfolio");
  
  // ดึงจำนวน Experience
  const expRes = await fetch("/api/profile/experience");
  
  // ดึงจำนวนข้อความที่ยังไม่อ่าน
  const msgRes = await fetch("/api/contact?unreadOnly=true");
  
  // อัปเดต State
  setStats({ portfolios, experiences, unreadMessages });
};
```

**เอาไว้ทำไร?**
- เป็นศูนย์กลางการจัดการเว็บไซต์
- เข้าถึงทุกฟังก์ชันได้จากที่เดียว
- แสดงข้อมูลสำคัญแบบเรียลไทม์

---

#### 📌 `/admin/about/page.tsx` - แก้ไขข้อมูล "เกี่ยวกับเรา"

**คืออะไร?**
หน้าสำหรับแก้ไขข้อมูลส่วนตัว ประวัติ ทักษะ และข้อมูลติดต่อ

**ส่วนประกอบ:**

##### 1. **ข้อมูลส่วนตัว**
```typescript
{
  name: "ชื่อ-นามสกุล",              // แสดงใน Hero Section
  description: "คำอธิบายสั้นๆ"      // แสดงใน Hero Section
}
```

**เอาไว้ทำไร?**
- แก้ไขชื่อที่แสดงในหน้าแรก
- เปลี่ยนคำแนะนำตัวสั้นๆ

##### 2. **ประวัติส่วนตัว**
```typescript
{
  bio: "ประวัติส่วนตัวยาว",         // แสดงใน About Section
  achievement: "ความสำเร็จ"         // แสดงใน About Section
}
```

**เอาไว้ทำไร?**
- เขียนประวัติตัวเองแบบละเอียด
- บอกความสำเร็จที่ภาคภูมิใจ

##### 3. **ทักษะ (Skills)**
```typescript
skills: [
  "HTML, CSS, JavaScript",
  "React, Next.js",
  "Node.js, Express",
  "..."
]
```

**รูปแบบการกรอก:**
```
JavaScript
TypeScript
React
Next.js
Node.js
```
(แต่ละบรรทัด = 1 ทักษะ)

**เอาไว้ทำไร?**
- แสดงทักษะที่มีในหน้า About/Skills Section
- แสดงเป็น List พร้อม Icon

##### 4. **ข้อมูลติดต่อ**
```typescript
{
  email: "example@email.com",
  phone: "091-826-6369",
  location: "Phuket, Thailand"
}
```

**เอาไว้ทำไร?**
- แสดงในหน้า Contact Section
- ให้คนติดต่อกลับได้

**ฟังก์ชันสำคัญ:**

```typescript
const handleSave = async () => {
  // แปลง Skills จากข้อความเป็น Array
  const skillsArray = formData.skills
    .split("\n")           // แยกตามบรรทัด
    .map(s => s.trim())    // ตัด Space
    .filter(s => s);       // เอาเฉพาะที่มีข้อความ

  // บันทึกผ่าน ProfileContext
  await updateProfile({
    name, description, bio, achievement,
    skills: skillsArray,
    email, phone, location
  });

  // บันทึกประวัติการแก้ไข
  await fetch("/api/admin/edit-history", {
    method: "POST",
    body: JSON.stringify({
      page: "About",
      action: "update",
      newValue: "Updated profile information"
    })
  });

  // แจ้งให้หน้าเว็บอัปเดต
  window.dispatchEvent(new Event("profileUpdated"));
};
```

**เอาไว้ทำไร?**
- บันทึกข้อมูลทั้งหมดพร้อมกัน
- อัปเดตหน้าเว็บแบบ Real-time
- บันทึกประวัติการแก้ไข

---

#### 📌 `/admin/education-experience/page.tsx` - จัดการการศึกษาและประสบการณ์

**คืออะไร?**
หน้าสำหรับจัดการข้อมูลการศึกษาและประสบการณ์การทำงาน

**ส่วนที่ 1: การศึกษา (Education)**

##### **มหาวิทยาลัย**
```typescript
{
  field: "สาขาวิชา",           // เช่น "วิทยาการคอมพิวเตอร์"
  university: "มหาวิทยาลัย",   // เช่น "มหาวิทยาลัยราชภัฏภูเก็ต"
  year: "ปีการศึกษา"           // เช่น "ปี 4"
}
```

##### **มัธยมศึกษา**
```typescript
{
  field: "แผนการเรียน",        // เช่น "คณิต-อังกฤษ"
  school: "โรงเรียน",          // เช่น "โรงเรียนเมืองถลาง"
  gpa: "เกรดเฉลี่ย"            // เช่น "3.50"
}
```

**เอาไว้ทำไร?**
- แสดงประวัติการศึกษาในหน้าเว็บ
- แก้ไขได้ทุกเมื่อ

**ส่วนที่ 2: ประสบการณ์การทำงาน (Experience)**

##### **ข้อมูลประสบการณ์**
```typescript
{
  title: "ตำแหน่งงาน",          // เช่น "Full Stack Developer"
  company: "บริษัท/องค์กร",     // เช่น "ABC Company"
  location: "สถานที่",          // เช่น "กรุงเทพฯ"
  period: "ช่วงเวลา",           // เช่น "2020 - 2023"
  description: "คำอธิบายงาน"   // เช่น "พัฒนา Web Application..."
}
```

**ฟังก์ชันสำคัญ:**

##### **1. เพิ่มประสบการณ์**
```typescript
const handleAddExperience = async () => {
  // ตรวจสอบข้อมูล
  if (!newExp.title || !newExp.company) {
    alert("กรุณากรอกข้อมูลให้ครบถ้วน");
    return;
  }

  // บันทึกลง Database
  await fetch("/api/profile/experience", {
    method: "POST",
    body: JSON.stringify(newExp)
  });

  // โหลดข้อมูลใหม่
  await loadExperiences();
  
  // ปิด Modal และ Clear Form
  setShowAddExp(false);
  setNewExp({ title: "", company: "", ... });
};
```

**เอาไว้ทำไร?**
- เพิ่มประสบการณ์ใหม่ลงในระบบ
- แสดงในหน้าเว็บทันที

##### **2. แก้ไขประสบการณ์**
```typescript
const handleUpdateExperience = async () => {
  await fetch("/api/profile/experience", {
    method: "PUT",
    body: JSON.stringify(editingExp)
  });

  await loadExperiences();
  setEditingExp(null);
};
```

**เอาไว้ทำไร?**
- แก้ไขประสบการณ์ที่มีอยู่
- อัปเดตหน้าเว็บทันที

##### **3. ลบประสบการณ์**
```typescript
const handleDeleteExperience = async (id: number) => {
  // ยืนยันก่อนลบ
  if (!confirm("คุณต้องการลบประสบการณ์นี้หรือไม่?")) return;

  await fetch(`/api/profile/experience?id=${id}`, {
    method: "DELETE"
  });

  await loadExperiences();
};
```

**เอาไว้ทำไร?**
- ลบประสบการณ์ที่ไม่ต้องการ
- ลบออกจากหน้าเว็บทันที

---

## 🔄 ขั้นตอนการทำงานของระบบ

### กรณีที่ 1: ลูกค้าส่งข้อความติดต่อ

```
1. ลูกค้ากรอกฟอร์มในหน้าเว็บ (page.tsx)
   ↓
2. กด "ส่งข้อความ"
   ↓
3. ส่งข้อมูลไปที่ /api/contact (POST)
   ↓
4. บันทึกลง ContactMessage table
   ↓
5. ส่ง Response กลับว่าสำเร็จ
   ↓
6. แสดง Alert "ส่งข้อความเรียบร้อยแล้ว"
   ↓
7. Admin เข้า /admin/messages เห็นข้อความใหม่
   ↓
8. Admin อ่านและทำเครื่องหมาย isRead = true
```

### กรณีที่ 2: Admin แก้ไขข้อมูล About

```
1. Admin เข้า /admin/about
   ↓
2. แก้ไขข้อความในช่อง Input/Textarea
   ↓
3. กด "บันทึก"
   ↓
4. ส่งข้อมูลไปที่ ProfileContext.updateProfile()
   ↓
5. updateProfile() เรียก /api/profile (PUT)
   ↓
6. บันทึกลง Profile table
   ↓
7. Dispatch event "profileUpdated"
   ↓
8. หน้าเว็บหลัก (page.tsx) ฟัง event และ refresh
   ↓
9. ดึงข้อมูลใหม่จาก /api/profile (GET)
   ↓
10. แสดงข้อมูลใหม่ในหน้าเว็บ
```

### กรณีที่ 3: Admin เปลี่ยนสีธีม

```
1. Admin เข้า /admin/theme
   ↓
2. เลือกสีจาก Color Picker
   ↓
3. เห็นตัวอย่างแบบ Live Preview
   ↓
4. กด "บันทึก"
   ↓
5. ส่งข้อมูลไปที่ /api/settings (PUT)
   ↓
6. บันทึกลง SiteSettings table
   ↓
7. หน้าเว็บหลักโหลดสีใหม่จาก /api/settings
   ↓
8. Apply สีใหม่ให้กับ CSS Variables
   ↓
9. เว็บไซต์เปลี่ยนสีทันที
```

---

## 💡 ทำไมต้องแยกไฟล์?

### ✅ ข้อดี:
1. **แยกหน้าที่ชัดเจน**
   - API จัดการ Database
   - Admin จัดการ UI/UX
   - Page.tsx แสดงผลให้คนทั่วไปดู

2. **แก้ไขง่าย**
   - ต้องการแก้ API → แก้แค่ `/api/contact/route.ts`
   - ต้องการแก้หน้า Admin → แก้แค่ `/admin/dashboard/page.tsx`

3. **ปลอดภัย**
   - API ตรวจสอบสิทธิ์ (Authentication)
   - Admin ต้อง Login ก่อน
   - คนทั่วไปดูแค่หน้า page.tsx

4. **Reusable**
   - API ใช้ซ้ำได้หลายหน้า
   - Component แยกชัดเจน

---

## 📚 สรุปการใช้งาน

### สำหรับ Admin:
1. เข้า `/admin/login` → ล็อกอิน
2. เข้า `/admin/dashboard` → เห็นภาพรวม
3. เลือกเมนูที่ต้องการแก้ไข
4. แก้ไขข้อมูล
5. กด "บันทึก"
6. ดูผลในหน้าเว็บ

### สำหรับลูกค้า:
1. เข้าหน้าเว็บหลัก `/`
2. เห็นข้อมูลที่ Admin แก้ไข
3. กรอกฟอร์มติดต่อ
4. กด "ส่งข้อความ"
5. Admin จะเห็นข้อความใน `/admin/messages`

---

## 🎯 ไฟล์ที่จะสร้างต่อ

- [ ] `/admin/portfolios/page.tsx` - จัดการผลงาน
- [ ] `/portfolio/[id]/page.tsx` - หน้ารายละเอียดผลงาน
- [ ] `/admin/messages/page.tsx` - ดูข้อความติดต่อ
- [ ] `/admin/editor/page.tsx` - แก้ไขเว็บแบบ Live
- [ ] `/admin/theme/page.tsx` - ตั้งค่าสีธีม
- [ ] อัปเดต `page.tsx` ให้เชื่อมกับระบบใหม่
- [ ] ลบหน้า Admin เดิม

---

**จะทำต่อในข้อความถัดไป! 🚀**

